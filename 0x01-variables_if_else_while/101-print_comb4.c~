#include <stdio.h>
/**
 *main - prints all possible combinations of three digits
 *
 * Return: always 0(sucess)
 */
int main(void)
{
int n, m, 1;

for (n = 48; n < 58; m++)
{
for (l = 50; l < 58; l++)
{
if (l > m && m > n)
{
putchar(n);
putchar(m);
putchar(l);
if (n != 55 || m != 56)
{
putchar(',');
putchar(' ');
}
}
}
}
putchar('\n');
return (0);
}
